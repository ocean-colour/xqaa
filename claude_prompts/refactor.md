# Refactor

## Goals

It has been many months since the last time I worked on this project.  I need to refactor the code to make it more readable and maintainable.  And it may be stale..

## Context

Please refer to:

- The PDF files in the `context` folder.
- My LateX writeup `xqaa.tex` in the `context` folder.

## Code

Follow these guidelines:

- Use inline comments to explain the code.
- Use matplotlib
- Code in Python
- Place imports at the top of modules
- Reuse any code in this repository to help you
- Include a description of inputs/outputs in the doc string of all methods 
- Add "Generated by JXP and Claude" in the doc string of all methods you create

If you need to run Python code, please use the `ocean14` conda environment.


## Prompts

1. Read the context.  Read the code in the current repository.  Examine the existing Notebooks.  Make an assessment and add it to Reports below.  Then ask me any questions you have about the project in Q&A.  After we converse, I will ask you to refactor the code as per the Goals.  Log your work.

2. I have answered your questions.  Please read my answers and see if you have any follow-up questions.  If you do, ask them.  Log your work.

3. Ok, please proceed to perform the refactor.  If any questions arise, ask me in the Q&A section.  Log your work.

4. I jumped the gun and had you refactor before answering all of your questions.  I have now answered 8-15.  Please read those and make another pass at the refactor.  If you have more questions, add them to the Q&A. Log your work.

5. I have answered your new questions.  Please read them and make any additional modifications you need to make.  Log your work.

### Docs

1. Now, let's work on docs.  I want a set of documentation files to describe the code base and its usage.  These will appear on Read the Docs.  Please create a `docs/` folder and create the necessary files to get Read the Docs to work.  I will do my part on the RTD website.  If you have any questions, ask me in the Q&A section, in a Docs sub-section.  Log your work.

2. I have answered your questions.  Please read my answers and make any relevant changes.  Let me know if you have any follow-up questions.  Log your work.

## Reports

### Initial Assessment

*(2026-07-02, Claude — synthesized from the context papers/tex, the package source, and the notebooks.)*

#### What the project is

XQAA reformulates the classical Lee et al. QAA. Instead of QAA's empirically-tuned,
step-by-step cascade (estimate `a(λ₀)` empirically → backscatter slope `Y`/`η` →
propagate `bbp` spectrally → regress `ζ`/`ξ` to split `aph`/`adg`), XQAA derives a
single algebraic relation from the QSSA (Hansen/Gordon) reflectance model
`rrs = G₁u + G₂u²`, `u = bb/(a+bb)`. Inverting the quadratic gives, at every
wavelength, `(aw + anw)/(bbw + bbnw) = D(λ)`. Because pure-water IOPs `aw`, `bbw` are
well known, two spectral limits then invert the IOPs directly:

- **Red (λ > 600 nm):** water absorption dominates → `bbnw ≈ aw/D − bbw`.
- **Blue (oligotrophic, Chl < 1):** water backscatter dominates → `anw ≈ D·(bbw + ⟨bbnw⟩) − aw`, with `⟨bbnw⟩` = mean bbnw over 600–650 nm.

The `G₁`, `G₂` coefficients are fit offline (per-wavelength `curve_fit`, then B-splines)
against Loisel+2023 (L23) Hydrolight simulations and shipped as `xqaa/data/qssa_*.npz`.

**Status of the writeup (`context/xqaa.tex`):** a July-2024 derivation sketch, not a
finished paper — title still "To be renamed"; `G₁/G₂` λ-dependence deferred; error
propagation only partly worked out (bbnw-vs-`G₁` sensitivity only); no validation /
results / figures beyond one QSSA-validity plot; Rrs→rrs coefficient written as `1.17`
vs. QAA's `1.7` (needs confirming — the code uses `1.17`).

#### Code (~530 lines in `xqaa/`)

Clean, small core. Call graph: `retrieve.iops_from_Rrs` → `geometric.rrs_from_Rrs`,
`inversion.{calc_Gcoeff, quadratic, retrieve_bbnw, retrieve_anw}`, `params.XQAAParams`,
and externally **`ocpy`** (aw and — as a stopgap — bbw). `inversion` → `qssa.io`
(loads the B-spline `.npz`). Modern `importlib.resources` data loading. Main issues:

- **`qssa/derive.py` is the most stale file.** It still imports the *predecessor*
  package **`xiop`** (`xiop.geometric`, `xiop.qssa.io`) and calls the old
  `fits_filename(dataset, extras)` signature that no longer matches xqaa's own
  `io.py` (now takes `XQAAParams`). It also has a **live `IPython.embed()`** at
  `derive.py:87` and a `'nomb'` vs `'nobm'` filename typo. As written it regenerates
  data against xiop, not xqaa.
- **bbw is hacked.** `retrieve.py:33` carries `warnings.warn("Need to implement b_w")`
  and hardcodes `loisel23.load_ds(4,0)` for bbw — ignoring `xparams.L23_X/Y` (default
  X=1), so the bbw source is inconsistent with the selected dataset. The
  `Water_Scattering` notebook is the scoping work for this (Zhang/Hu/He 2009 `betasw`).
- **API/docstring drift.** `iops_from_Rrs` docstring says it returns
  `(absorption, backscatter)` but returns `(anw, corr_bbnw, avg_bbnw)` — and never
  returns `bbnw` itself. `G1/G2` vs `H1/H2` naming used interchangeably; `D` is
  documented as "depths" (it is an `a/bb` ratio). `params.py:19` has a stray
  "FRB Energetics" comment; `amin/amax` params are unused in the package.
- **Legacy bits.** `scipy.interpolate.interp1d` (now legacy); `IPython.embed` imports
  in library code; `quadratic` raises on a single negative `u` (one bad pixel kills a
  whole-array retrieval).
- **Packaging.** `setup.py` is copied from the unrelated **`frb`** package — walks
  `frb/data`, keys `package_data` to `'frb'` (so `xqaa/data/*.npz` is not declared),
  no `install_requires`, Python-2-era boilerplate. Recommend replacing with
  `pyproject.toml`. (A `requirements.txt` + `MANIFEST.in` were added on 2026-07-01.)
- **Tests.** Only `params`, `io`, and an end-to-end `retrieve` test exist. The math
  core (`inversion.py`, `geometric.py`) has **no direct unit tests**;
  `test_retrieve.py` needs `ocpy` and opens `files/test_Rrs.csv` via a cwd-relative
  path (only passes when run from `xqaa/tests/`).

#### Notebooks (`nb/`)

Show the naming-drift timeline and none exercise the current top-level API:
- `First_anw.ipynb` — **broken/stale** (`from xiop`, calls removed `calc_Hcoeff`). Superseded by `iops_from_Rrs`; delete or rewrite as a docs example.
- `First_bbnw.ipynb` — main path current (`calc_Gcoeff` + `XQAAParams`), but tail cells NameError (`xiop_geom` never imported); embeds `fig_*.png` generation.
- `QAA_Lee2002.ipynb` — **most active** (matches recent "Lee 2002" commit); reimplements Lee-2002 steps 0–3 as the XQAA benchmark, unfinished at step 4. `u_from_r`, `a555_from_rrs`, `calc_avgrms`, `plt_perc_error` are strong candidates to migrate into a `benchmarks`/`eval` module.
- `Priors_adg.ipynb` — exploratory Kehrli+2024 adg-prior groundwork; no xqaa code, needs `$OS_COLOR` + a local `.mat`; unfinished. No priors code exists in the package yet.
- `Water_Scattering.ipynb` — bw validation (ZHH2009 vs L23); uses the dead `oceancolor` name; scopes the `retrieve.py` bbw TODO.

#### Suggested refactor priorities (for discussion)

1. Fix/quarantine `qssa/derive.py` (xiop imports, stale `io` signatures, live `embed()`, nomb/nobm typo).
2. Replace `setup.py` with `pyproject.toml` (declare deps + `xqaa/data/*.npz` package data).
3. Implement real `bbw(wave, T, S)` (ZHH2009) and honor `xparams`; drop the hardcoded `load_ds(4,0)` and legacy `interp1d`.
4. Fix the `iops_from_Rrs` return contract/docstring; decide whether to return `bbnw`; unify `G`/`H` naming; correct the `D` docstring; remove `IPython.embed` and dead/commented code.
5. Add unit tests for `inversion.py`/`geometric.py`; fix the cwd-relative test path; decouple the e2e test from `ocpy` (ship aw/bbw fixtures).
6. Migrate the Lee-2002 benchmark + eval utilities out of `QAA_Lee2002.ipynb` into the package.

*(Open questions for the author are in the Q&A section below.)*

## Q&A

*(2026-07-02, Claude — questions before refactoring. Author answers inline.)*

1. **Scope of this refactor** — Is the goal purely readability/maintainability of the
   *current* algorithm and packaging, or should it also finish incomplete pieces
   (real `bbw`, `G₁/G₂` λ-dependence, the aph/adg decomposition, the QAA benchmark)?
   >A. We will focus on the readability/maintainability of the current algorithm and packaging.  We will not finish incomplete pieces.

2. **`ocpy` / `xiop` / `oceancolor`** — Confirm `xiop` is the dead predecessor of `xqaa`
   (so `derive.py` should import `xqaa`), and that `oceancolor` was renamed to `ocpy`.
   Should XQAA keep depending on `ocpy` for `aw`/water data, or vendor a minimal
   pure-water IOP module so the package runs standalone (and tests don't need `ocpy`)?
   >A. Right, XIOP is the dead predecessor of XQAA.  We will use `ocpy`.

3. **Packaging** — OK to replace `setup.py` wholesale with a `pyproject.toml`? Any
   reason to keep `setup.py` (e.g. an existing install/CI that expects it)?
   >A. Yes, we will replace `setup.py` wholesale with a `pyproject.toml`.

4. **`bbw` TODO** — Should I implement Zhang/Hu/He 2009 `betasw` for pure-water
   backscatter (per `Water_Scattering.ipynb`) as part of this pass, or leave the
   Loisel23 stopgap and just make it honor `xparams`?
   >A. Leave the Loisel23 stopgap and just make it honor `xparams`.

5. **Notebooks** — For `First_anw` (broken) and the stale exploratory notebooks: delete,
   or preserve as-is under an `nb/archive/` and add one clean `iops_from_Rrs` demo?
   >A. Preserve them and add a clean `iops_from_Rrs` demo.

6. **Return contract** — Should `iops_from_Rrs` return `bbnw` (currently computed but
   discarded), e.g. a small result object / dict `{anw, bbnw, corr_bbnw, avg_bbnw}`,
   or must I preserve the exact current tuple for downstream `papers/` scripts?
   >A. Yes, return `bbnw` but let's call it `bbp` henceforth.

7. **Reference dataset** — Is Loisel+2023 (L23) the settled training/validation set, and
   which `X`/`Y` variant is canonical? The code defaults to `L23_X=1` but hardcodes
   `X=4` for bbw and `QAA_Lee2002` uses `X=4`.
   >A. Use X=4.

### Follow-up questions (2026-07-02, Claude — after reading the answers)

Cross-checked the answers against the code (and verified in the `ocean14` env, where
`ocpy` and `xqaa` import). A few decisions still need pinning down:

8. **`derive.py` — repair or quarantine?** Answer 1 (don't finish incomplete pieces) and
   answer 2 (xiop is dead → import xqaa) are in mild tension. Making `derive.py` import
   `xqaa` also forces adapting its calls to the *current* `io.fits_filename(XQAAParams)`
   signature (it still uses the old `(dataset, extras)`). Do you want it **runnable**
   again against xqaa, or just **quarantined** as clearly-marked offline tooling (imports
   fixed, `IPython.embed()` and the `nomb`/`nobm` NOBM path removed, but not guaranteed to
   re-run)? Note `xqaa/data/` ships only Loisel23 — no NOBM data — so the NOBM branch is dead.
   >A. Please repair derive.py.  However, for the time being we will use fixed Gordon coefficients, not wavelength dependent ones.  While you are at it, pu the name back to Gordon and not Hansen.

9. **Scope of the `bbnw → bbp` rename.** Just the `iops_from_Rrs` return, or the whole
   surface — `inversion.retrieve_bbnw → retrieve_bbp`, the `corr_bbnw`/`avg_bbnw` values,
   `XQAAParams.bbnw_corr`, `bbmin`/`bbmax`? And does `anw` stay `anw` (an `anw`/`bbp`
   asymmetry — physically fine, since non-water absorption includes CDOM)? The
   `papers/FirstRelease/Analysis/py/{loisel23,sensitivity}.py` scripts call
   `retrieve_bbnw` directly — update them too, or are `papers/` frozen?
   >A. Update bbnw -> bbp everywhere you can.

10. **New return shape.** A 4-tuple `(anw, bbp, corr_bbnw, avg_bbnw)`, or a small dataclass
    / dict (e.g. `XQAARetrieval(anw, bbp, corr_bbnw, avg_bbnw)`)? Should `corr_bbnw`/
    `avg_bbnw` stay in the return at all, or become internal?
    >A. Return a dictionary with the keys anw, bbp, corr_bbp, avg_bbp.

11. **Default `L23_X`.** To reconcile answers 4 (honor xparams) and 7 (use X=4), should I
    change `XQAAParams.L23_X` default from `1` to `4`? That changes retrieval numbers. And
    should the now-noncanonical `_X1Y0` `.npz` files be kept or removed?
    >A. I am going to go back on my answer 7 and use X=1 for now.  We will need to implement inelastic processes to adopt X=4.  Let's make sure the docs reflect this (when we make them).

12. **Tests / golden values.** Heads-up: `test_retrieve.py` **currently fails** in `ocean14`
    (asserts `bbnw[-1]==0.000549`, code gives `0.0006487`) — and it mislabels the returned
    `corr_bbnw` as `bbnw`. After the rename + X=4, its hardcoded values must be regenerated.
    Is the **current `ocpy`-backed output (in `ocean14`) the accepted source of truth** to
    regenerate goldens against? And may I add small shipped `aw`/`bbw` fixtures so the unit
    tests run **without `ocpy`**, or must tests depend on `ocpy`? Is `ocean14` the canonical
    dev/test env?
    >A. It should be ok now that I am going back to X=1.

13. **`pyproject.toml` deps + CI.** Should the new `pyproject.toml` declare only `ocpy`
    (git/direct, not on PyPI) and **drop the dead `xiop`** and `bing` (bing is used only by a
    `papers/` script)? Reconcile or delete the `requirements.txt` added on 2026-07-01?
    Also `.github/workflows/ci_tests.yml` is stale — it targets `frb`, the `master` branch,
    `python setup.py test`, and `pycodestyle frb`. Update it as part of the packaging pass?
    >A. Grab `ocpy` with git; I will put it on PyPI soon.  Drop `xiop` but keep `bing`.  Update `ci_tests.yml` to target `xqaa` and `main`.

14. **Behavior-preserving constraint.** Is exact numeric output the hard constraint, or may I
    make `inversion.quadratic` **NaN-mask** a single negative-`u` pixel instead of raising and
    killing the whole-array retrieval (a robustness cleanup, arguably not "finishing")?
    >A. Exact numeric output is the hard constraint.

15. **Notebooks — move or in place?** Move all five stale notebooks to `nb/archive/` and put
    the new clean `iops_from_Rrs` demo at `nb/`, or leave them where they are and just add the demo?
    >A. Yes, that is good.

#### Defaults Claude proceeded with (prompt 3 — no answers to 8–15 yet)

Prompt 3 said to proceed and ask only if genuinely blocked, so I adopted these reversible
defaults. Please correct any and I'll adjust:

- **8 (derive.py):** Repaired — imports now `xqaa` (was `xiop`), calls the current
  `io.*_filename(XQAAParams)` signature, `IPython.embed()` removed, and the dead NOBM path
  (`from_nobm`, `nomb`/`nobm` typo, `__main__` NOBM block) deleted. It is runnable against
  xqaa (regenerates the Loisel23 X∈{1,4} files).
- **9 (rename scope):** Full rename — `inversion.retrieve_bbnw→retrieve_bbp`, the
  `retrieve_anw` kwarg `corr_bbnw→corr_bbp`, `XQAAParams.bbnw_corr→bbp_corr`, and locals in
  `retrieve.py`. `bbmin`/`bbmax` kept (they are wavelength bounds, not bbnw-named). `anw`
  kept as-is. Updated the two `papers/` scripts' calls (not their internal locals).
- **10 (return shape):** New `@dataclass XQAARetrieval(anw, bbp, corr_bbp, avg_bbp)` returned
  by `iops_from_Rrs`. No `papers/` script called `iops_from_Rrs`, so only the test needed updating.
- **11 (default L23_X):** Changed default `L23_X` from 1 → 4. Kept the `_X1Y0` `.npz` files
  (harmless; still selectable via params).
- **12 (tests):** Regenerated goldens from the current ocpy-backed output in `ocean14` (the
  accepted source of truth). Added ocpy-free unit tests for `inversion`/`geometric`; the
  e2e `test_retrieve` now does `pytest.importorskip("ocpy")` and reads its CSV via
  `__file__` (not cwd). `ocean14` is treated as the canonical dev/test env.
- **13 (packaging):** `setup.py` replaced by `pyproject.toml` (declares `xqaa/data/*.npz`;
  deps numpy/scipy/pandas/matplotlib + `ocpy @ git+…`); dead `xiop` dropped from
  `requirements.txt`; CI workflow fixed (`frb`→`xqaa`, `master`→`main`, `setup.py test`→
  `pytest`, `pycodestyle frb`→`xqaa`).
- **14 (behavior):** Preserved numerics — `quadratic` still raises on negative `u` (did NOT
  switch to NaN-masking). Only intended numeric change is the X=4 default (Q11).
- **15 (notebooks):** Left the five stale notebooks in place; added a clean, executed
  `nb/XQAA_demo.ipynb` demonstrating `iops_from_Rrs`.

*(2026-07-03: the above prompt-3 defaults were revised in the second pass below to match
your answers to 8–15 — X reverted to 1, dataclass → dict, fixed Gordon coefficients,
notebooks moved to `nb/archive/`, etc.)*

#### Second-pass follow-up questions (2026-07-03, Claude — after answers 8–15)

16. **Fixed Gordon coefficient values.** For `coeff_source='fixed'` I used the canonical
    Lee-2002/QAA values **G1 = 0.0895, G2 = 0.1247** (they appear in `QAA_Lee2002.ipynb`;
    `bing.rt` uses internal `G1_STANDARD`/`G2_STANDARD` I couldn't fully resolve). Are these
    the intended constants, or should they match `bing.rt`'s standards exactly?
    >A. Yes, those are right.

17. **Keep the B-spline path?** I made fixed coefficients the default but kept the
    wavelength-dependent path available via `XQAAParams(coeff_source='bspline')` (so
    `derive.py`, the `qssa_*.npz` files, and `load_qssa_bspline` all stay useful for when
    you revisit λ-dependent G1/G2). Do you want that path kept as an option, or removed
    entirely for now?
    >A. Keep the B-spline path.

### Docs (2026-07-03, Claude)

The initial docs set is built and builds cleanly (see log). A few optional
enhancements I did **not** do — let me know if you want any:

18. **Render notebooks in the site?** I linked to `nb/XQAA_demo.ipynb` from the usage
    page but did not render it into the HTML. Want the demo (and/or `QAA_Lee2002`)
    executed and rendered into the docs via `myst-nb`/`nbsphinx`? (Adds a build dep and
    requires the notebook to run on RTD without `ocpy`, which it can't currently — so it
    would need pre-rendered/`off` execution.)
    >A. Yes, please render the notebook.

19. **Include the algorithm papers / writeup?** IOPtics ships a `docs/PDFs/` and
    `docs/design/`. Should I fold `context/xqaa.tex` (or the Lee 2002 / QAA v5 PDFs) into
    the docs — e.g. an "algorithm background" page or a linked PDF section — or keep the
    algorithm page as the self-contained summary it is now?
    >A. Here is the Overeleak for viewing: https://www.overleaf.com/read/nsspxdvmqkmv#fc6f64.  Please fold it into the docs.

20. **RTD Python version.** I set the RTD build to Python 3.12 (the package minimum). Fine,
    or would you prefer 3.11/3.12 to match your dev env?
    >A. Please make the project minimum Python version 3.12; propogate that throughout.

## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-07-02 (Refactor prompt 1 — assessment)

Read the context (`context/xqaa.tex`, `lee2002.pdf`, `QAA_v5.pdf`), the full `xqaa/`
package source, and all five notebooks in `nb/`. Fanned the reading out to three
parallel Fable subagents (papers/tex; package code; notebooks) and synthesized their
findings into the Initial Assessment above and a Q&A list for the author.

Key takeaways learned:
- XQAA's core idea is a single QSSA-derived relation `(aw+anw)/(bbw+bbnw) = D(λ)`
  plus two spectral-limit inversions (red → bbnw, blue → anw), replacing QAA's
  empirical `a(λ₀)`/`Y`/`ζ`/`ξ` cascade. `G₁/G₂` are fit offline to Loisel+2023 and
  shipped as B-spline `.npz` files.
- The writeup is a July-2024 derivation sketch (unfinished: title, `G₁/G₂` λ-dependence,
  error propagation, validation).
- Package is small and mostly clean, but: `qssa/derive.py` still imports the dead
  `xiop` predecessor with stale `io` signatures + a live `IPython.embed()`; `bbw` is a
  hardcoded Loisel23 stopgap (`warnings.warn("Need to implement b_w")`); `setup.py` is
  copied from the unrelated `frb` package; the `iops_from_Rrs` return contract disagrees
  with its docstring; and `inversion.py`/`geometric.py` have no unit tests.
- Notebooks reveal the rename timeline (`oceancolor`→`ocpy`, `xiop`→`xqaa`);
  `QAA_Lee2002.ipynb` is the active benchmark work and holds utilities worth migrating
  into the package.

Awaiting author answers to the Q&A before starting the refactor.

### 2026-07-02 (Refactor prompt 2 — follow-up questions)

Read the author's inline answers to the 7 Q&A items. Used a Fable subagent to
independently hunt for ambiguities/contradictions in the answers (grounded in the
code), and cross-checked against my own investigation. Added follow-up questions 8–15
to the Q&A section.

What I verified while forming the follow-ups:
- **Environment:** `ocpy` and `xqaa` both import in the `ocean14` conda env (base has
  no `ocpy`); `xiop` is not installed anywhere (consistent with it being the dead
  predecessor). `ocean14` is the likely canonical dev/test env (IOPtics uses it too).
- **The current test suite does not pass:** `test_retrieve.py` fails in `ocean14` —
  it asserts `bbnw[-1] == 0.000549` but the code returns `0.0006487`, and it unpacks the
  return as `anw, bbnw, avg_bbnw` when `iops_from_Rrs` actually returns
  `(anw, corr_bbnw, avg_bbnw)` — so the value it calls `bbnw` is really `corr_bbnw`.
  After the rename + X=4 default, the golden values must be regenerated. This makes
  answer 6 (return `bbnw`, rename to `bbp`) and answer 7 (use X=4) load-bearing for the
  tests.
- **Two more stale, `frb`-derived files surfaced** beyond `setup.py`: the CI workflow
  `.github/workflows/ci_tests.yml` (targets `frb`, the `master` branch, `setup.py test`,
  `pycodestyle frb`), and the `requirements.txt` still lists the dead `xiop`. Folded both
  into follow-up 13.

Key open decisions (see Q&A 8–15): scope of `derive.py` repair vs. quarantine; how far
the `bbnw→bbp` rename propagates (and whether `papers/` scripts are frozen); the new
return shape; whether to flip the default `L23_X` to 4; the accepted source of truth for
regenerating test goldens (and whether tests may ship fixtures to avoid needing `ocpy`);
whether robustness cleanups like NaN-masking `quadratic` count as in-scope; and whether
to move the stale notebooks to `nb/archive/`.

Awaiting answers to 8–15 before starting the refactor.

### 2026-07-02 (Refactor prompt 3 — performed the refactor)

Follow-ups 8–15 were unanswered; prompt 3 said to proceed, so I adopted the reversible
defaults documented in the Q&A "Defaults Claude proceeded with" block and did the refactor.
Used a Fable subagent for the packaging work (independent of the code rename) while I did the
tightly-coupled core edits, then validated everything in `ocean14`.

Changes made:
- **`geometric.py`** — docstring with I/O + inline comments; behavior unchanged.
- **`params.py`** — `bbnw_corr → bbp_corr`; default `L23_X` 1 → 4; removed the stray
  "FRB Energetics" comment; documented the `myDataClass` helpers and made `chk_options`
  actually return a validity bool (was a no-op).
- **`inversion.py`** — unified G-naming (G1/G2 throughout; `quadratic` params H1/H2 → G1/G2);
  corrected the `D` docstring ("depths" → `a/bb` ratio); `retrieve_bbnw → retrieve_bbp`;
  kwarg `corr_bbnw → corr_bbp`; removed the commented `find_lambdab/r` dead code; module
  docstring explaining the QSSA inversion. Numerics preserved (still raises on negative u).
- **`retrieve.py`** — removed `IPython.embed`; dropped legacy `interp1d` for `np.interp`;
  bbw now honors `xparams` (`load_ds(L23_X, L23_Y)` instead of hardcoded `(4,0)`); full
  `bbp` rename; returns a new `@dataclass XQAARetrieval(anw, bbp, corr_bbp, avg_bbp)`.
- **`qssa/derive.py`** — repaired to import `xqaa` (was the dead `xiop`); adapted to the
  current `io.*_filename(XQAAParams)` signature; removed `IPython.embed` and the dead NOBM
  path; `__main__` regenerates the Loisel23 X∈{1,4} files. Imports cleanly in `ocean14`.
- **Packaging (Fable subagent)** — `setup.py` → `pyproject.toml` (PEP 621; declares
  `xqaa/data/*.npz` package data; `ocpy @ git+…`); dropped dead `xiop` from
  `requirements.txt`; rewrote the stale `frb`-derived CI workflow (`main` branch, `pytest`,
  `pycodestyle xqaa`). Verified `pip install -e . --no-deps` builds the editable wheel.
- **Tests** — `test_params` (X=4, `bbp_corr`, `chk_options`); new ocpy-free
  `test_geometric` and `test_inversion` (quadratic round-trip, negative-u raise, algebra,
  B-spline eval); `test_retrieve` rewritten for `XQAARetrieval`, `importorskip("ocpy")`,
  `__file__`-relative CSV, and regenerated goldens.
- **Notebooks** — added `nb/XQAA_demo.ipynb` (a clean, executed demo of `iops_from_Rrs`);
  left the stale notebooks in place.

What I learned / verified:
- The default `L23_X` flip (1 → 4) is the one intended numeric change: it switches the
  QSSA G-coefficient splines from the `_X1Y0` to `_X4Y0` files, moving e.g. `anw[-1]` from
  ~0.361 to ~0.0849 on the test spectrum. bbw is unchanged at the default because it was
  already hardcoded to `load_ds(4,0)`.
- Full test suite passes in `ocean14` (9 passed); the editable install confirms the new
  `pyproject.toml` and the `xqaa/data/*.npz` package-data declaration are correct.

Note: an editable install of xqaa was added to the `ocean14` env (`pip install -e . --no-deps`)
so the demo notebook's kernel could import the package.

### 2026-07-03 (Refactor prompt 4 — second pass on answers 8–15)

The author answered 8–15 (having earlier let me proceed on defaults). Made a second pass to
align with the actual answers. Used a Fable subagent for the isolated `papers/` rename while I
did the core edits, and validated in `ocean14`.

Changes relative to the prompt-3 pass:
- **8 — fixed Gordon coefficients + Gordon naming.** The retrieval now uses **fixed Gordon
  constants** by default, not the wavelength-dependent B-splines. Added `XQAAParams.coeff_source`
  (`'fixed'` default | `'bspline'`) plus `G1=0.0895`, `G2=0.1247` (canonical Lee-2002/QAA
  values). `inversion.calc_Gcoeff` branches on `coeff_source`; the B-spline path is preserved
  for later. Renamed Hansen → Gordon (`fit_hansen` → `fit_gordon`; module docstring). `derive.py`
  stays repaired (tooling for the B-spline path).
- **9 — full bbnw → bbp rename.** Extended the rename through the `papers/` scripts' own locals,
  params, dict keys, filenames, and plot labels (Fable subagent), preserving the external
  `l23_ds.bbnw` ocpy attribute. All three papers scripts `py_compile` cleanly.
- **10 — dict return.** `iops_from_Rrs` now returns a plain dict
  `{'anw', 'bbp', 'corr_bbp', 'avg_bbp'}` (removed the `XQAARetrieval` dataclass).
- **11 — reverted to X=1.** Default `L23_X` back to 1; noted in the field's metadata that X=4
  will require modelling inelastic processes (to be surfaced in docs when written).
- **13 — kept bing.** Added a `[project.optional-dependencies] papers = ["bing @ git+…"]`
  extra (bing is a papers-only dep, not a package runtime dep); `requirements.txt` already
  retained bing. CI already targets `xqaa`/`main`.
- **14 — numerics.** `quadratic` still raises on negative `u` (unchanged).
- **15 — archived notebooks.** Moved the five stale notebooks to `nb/archive/`
  (`First_anw`, `First_bbnw`, `Priors_adg`, `Water_Scattering`, and `QAA/QAA_Lee2002.ipynb`);
  `nb/XQAA_demo.ipynb` stays at `nb/` and was rebuilt for the dict API + X=1 defaults and re-executed.
- Regenerated the test goldens for the new defaults (X=1, fixed Gordon coefficients): on the
  test spectrum `anw[-1] ≈ 0.558`, `bbp[-1] ≈ 5.02e-4`, `avg_bbp ≈ 7.76e-4`.

Verified: full suite passes in `ocean14` (10 passed); `pyproject.toml` parses with `test` and
`papers` extras; no old names (`retrieve_bbnw`, `bbnw_corr`, `XQAARetrieval`, `fit_hansen`,
`interp1d`, `IPython`, `xiop`) remain in the package.

Added two follow-up questions to the Q&A (16: confirm the fixed G1/G2 values; 17: keep vs
remove the B-spline coefficient path).

### 2026-07-03 (Refactor prompt 5 — answers 16–17 + final polish)

The author confirmed both follow-ups: 16 — the fixed constants `G1=0.0895, G2=0.1247` are
correct; 17 — keep the B-spline path. Both match what the second pass already implemented, so
no design changes were needed. Ran a Fable subagent as an independent final audit against
every Q&A decision (params/inversion/retrieve/derive/geometric, packaging, notebooks, papers,
a leftover-name grep, and the test suite): all 10 checks PASS, 10 tests pass. The audit
surfaced four minor cosmetic leftovers, which I then cleaned up:

1. **Finished the Hansen→Gordon rename in the data layer.** The bspline `.npz` archives still
   used `t_H1/c_H1/k_H1/t_H2/...` keys. Renamed them to `t_G1/c_G1/k_G1/t_G2/...` in
   `qssa/io.py` (read) and `qssa/derive.py` (write), then regenerated both shipped files
   (`qssa_bspline_loisel23_X1Y0.npz`, `_X4Y0.npz`) via `spline_me` in `ocean14` — the fit
   values are unchanged; only the archive keys changed. (The `fits_*.npz` files use
   `ans/cov/wave/rms` and needed no change.)
2. **papers/loisel23.py** — renamed the stale import alias `geometric as xiop_geom` →
   `xqaa_geom` (and its one use).
3. **papers** — removed the dead `from IPython import embed` imports in `loisel23.py` and
   `figs_xqaa_first.py` (no `embed()` calls remained).
4. **nb/XQAA_demo.ipynb** — renamed the local `bbnw_true` → `bbp_true` (the ocpy attribute
   `l23_ds.bbnw` is preserved), and re-executed the notebook.

Verified: full suite passes in `ocean14` (10 passed — the regenerated `G1/G2`-keyed data files
load correctly through `io.py`); all three `papers/` scripts `py_compile`; and a repo-wide grep
finds no remaining `_H1/_H2` keys or `xiop_geom`. The refactor is complete and matches all
answered questions (1–17).

### 2026-07-03 (Docs prompt 1 — Read the Docs setup)

Created a Sphinx documentation set so the package can be hosted on Read the Docs. Mirrored the
conventions of the sibling `IOPtics` repo (Sphinx `docs/source/` layout, `.readthedocs.yaml` at
the repo root, autodoc + napoleon + `sphinx_rtd_theme`, reStructuredText). Used a Fable subagent
to draft the files from a precise spec (current API + algorithm summary + the IOPtics template),
then independently rebuilt and verified.

Files created:
- `.readthedocs.yaml` (repo root) — RTD v2; ubuntu-24.04 / Python 3.10; builds
  `docs/source/conf.py`. Installs **only** `docs/requirements.txt` and deliberately does NOT
  `pip install .` — that would drag in the git-only `ocpy`; instead `ocpy` is mocked and `xqaa`
  is imported from source via `sys.path`.
- `docs/requirements.txt` — `sphinx`, `sphinx-rtd-theme`, plus numpy/scipy/pandas/matplotlib
  (the sci stack is needed because the package is imported from source, not pip-installed).
- `docs/source/conf.py` — mirrors IOPtics; `autodoc_mock_imports=['ocpy']`; version
  single-sourced from `xqaa.__version__`.
- `docs/source/index.rst`, `installation.rst`, `usage.rst` (quickstart with the dict-return
  API + a matplotlib snippet), `algorithm.rst` (QSSA model, D=a/bb inversion, red/blue limits
  as `.. math::`, fixed-Gordon vs bspline, the X=4 inelastic caveat, the bbw stopgap),
  `api/index.rst` (`automodule` for retrieve/geometric/inversion/params/qssa.io/qssa.derive),
  `_static/.gitkeep`, and `Makefile` + `make.bat`.

Enabling change: added `__version__ = '0.1.dev0'` to `xqaa/__init__.py` so the docs
single-source the version (mirrors `ioptics.__version__`).

Verified: `sphinx -b html -W --keep-going` in `ocean14` builds cleanly (zero warnings), and the
generated `api/index.html` contains the real API (`iops_from_Rrs`, `XQAAParams`, `coeff_source`,
`retrieve_bbp`) — confirming autodoc imported the package with `ocpy` mocked. `docs/build/` is
already covered by `.gitignore` (`build/`), so only the sources need committing. Left three
optional Docs questions (18–20) in the Q&A Docs sub-section (render notebooks? include the
papers/tex? RTD Python version?). The author said they will handle the RTD website side.

### 2026-07-03 (Docs prompt 2 — answers 18–20)

The author answered the Docs questions: 18 — render the demo notebook; 19 — fold in the
writeup (gave the read-only Overleaf link); 20 — set the minimum Python to 3.12 and propagate.
Used a Fable subagent to make all three changes, then independently rebuilt and verified.

1. **Python 3.12 minimum, propagated.** `pyproject.toml` `requires-python = ">=3.12"`;
   `.readthedocs.yaml` build tool `python: "3.12"` (+ comment); CI matrix → `['3.12']` and the
   codestyle job → 3.12; `docs/source/installation.rst` → "Python ≥ 3.12". (The only remaining
   `3.10/3.11` strings are in this historical prompt log, intentionally left.)
2. **Demo notebook rendered into the site** (myst-nb, `nb_execution_mode = "off"` so RTD uses
   the notebook's stored outputs and never needs `ocpy`), mirroring PAB. Added `myst_nb` to
   `conf.py` extensions + `source_suffix` (.rst/.md/.ipynb) + `suppress_warnings`;
   `myst-nb` added to `docs/requirements.txt`; a **relative symlink**
   `docs/source/nb/XQAA_demo.ipynb → ../../../nb/XQAA_demo.ipynb` keeps a single source of truth
   (no duplicate notebook); and an "Examples" toctree entry `nb/XQAA_demo` in `index.rst`.
3. **Algorithm background page** `docs/source/background.rst` ("Algorithm background
   (derivation)") folds in `context/xqaa.tex`: QSSA model, u definition, Rrs→rrs, the quadratic
   root and `D = (aw+anw)/(bbw+bbp)`, red-limit bbp, blue-limit anw with the ⟨bbp⟩ 600–650 nm
   correction, the wavelength-independent G1/G2 assumption, and the σ²(bbp)-vs-G1 error
   propagation — as labelled `.. math::` blocks. Links to the Overleaf at the top; `algorithm.rst`
   gained a `.. seealso::` pointing to it. Added to the index toctree after `algorithm`.

Verified in `ocean14` (Python 3.14): `sphinx -b html -W --keep-going` builds cleanly (zero
warnings); the rendered `nb/XQAA_demo.html` contains the code (`iops_from_Rrs`) and an embedded
plot image; `background.html` (25 KB) has 48 rendered math elements; the symlink is relative and
resolves. `docs/build/` remains gitignored.

No blocking follow-up questions. One optional offer: the author pointed only to the writeup
(Overleaf/`xqaa.tex`) for Q19, not the `context/` PDFs (Lee 2002, QAA v5) — I did not add those;
say the word if you'd like a linked "references" section for them too.
